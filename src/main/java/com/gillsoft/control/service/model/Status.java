package com.gillsoft.control.service.model;

public enum Status {

	NEW,
	RESERVE,
	CANCEL,
	CONFIRM,
	RETURN,
	NEW_ERROR,
	RESERVE_ERROR,
	CANCEL_ERROR,
	CONFIRM_ERROR,
	RETURN_ERROR
	
}
